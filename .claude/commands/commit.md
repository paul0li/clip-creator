Review all staged and unstaged changes, then create a single commit with a one-liner message using conventional commit format (e.g. `fix: ...`, `refactor: ...`, `feat: ...`, `docs: ...`, `chore: ...`).

Rules:
- Message must be a single line, lowercase, no period at the end
- Do NOT add the `Co-Authored-By` trailer or any signature
- Stage only relevant files (no `.env`, credentials, or secrets)
- If there are no changes to commit, say so and stop
